import 'package:flutter/material.dart';

class InscribirRepresentante extends StatelessWidget {
  const InscribirRepresentante({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Inscribir Representante');
  }
}