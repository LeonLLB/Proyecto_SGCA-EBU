import 'package:flutter/material.dart';

class VisualizarRepresentante extends StatelessWidget {
  const VisualizarRepresentante({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Visualizar Representante');
  }
}