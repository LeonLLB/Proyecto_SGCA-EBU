import 'package:flutter/material.dart';

class ActualizarRepresentante extends StatelessWidget {
  const ActualizarRepresentante({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Actualizar Representante');
  }
}