import 'package:flutter/material.dart';

class BuscarRepresentante extends StatelessWidget {
  const BuscarRepresentante({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Buscar Representante');
  }
}