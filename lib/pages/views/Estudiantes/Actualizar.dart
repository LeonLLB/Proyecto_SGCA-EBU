import 'package:flutter/material.dart';

class ActualizarEstudiante extends StatelessWidget {
  const ActualizarEstudiante({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Actualizar estudiante');
  }
}