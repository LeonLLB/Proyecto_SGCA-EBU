import 'package:flutter/material.dart';

class SubirRendimientoEstudiante extends StatelessWidget {
  const SubirRendimientoEstudiante({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('SubirRendimiento estudiante');
  }
}