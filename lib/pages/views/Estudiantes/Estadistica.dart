import 'package:flutter/material.dart';

class EstadisticaEstudiante extends StatelessWidget {
  const EstadisticaEstudiante({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Estadistica estudiante');
  }
}