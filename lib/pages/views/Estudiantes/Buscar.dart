import 'package:flutter/material.dart';

class BuscarEstudiante extends StatelessWidget {
  const BuscarEstudiante({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Buscar estudiante');
  }
}