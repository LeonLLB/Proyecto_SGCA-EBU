import 'package:flutter/material.dart';

class MatriculaEstudiante extends StatelessWidget {
  const MatriculaEstudiante({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Matricula estudiante');
  }
}