import 'package:flutter/material.dart';

class InscribirEstudiante extends StatelessWidget {
  const InscribirEstudiante({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Inscribir estudiante');
  }
}