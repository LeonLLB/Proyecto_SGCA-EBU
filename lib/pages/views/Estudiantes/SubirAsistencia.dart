import 'package:flutter/material.dart';

class SubirAsistenciaEstudiante extends StatelessWidget {
  const SubirAsistenciaEstudiante({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('SubirAsistencia estudiante');
  }
}