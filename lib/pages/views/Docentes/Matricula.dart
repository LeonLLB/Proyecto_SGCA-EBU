import 'package:flutter/material.dart';

class MatriculaDocente extends StatelessWidget {
  const MatriculaDocente({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Matricula Docente');
  }
}