import 'package:flutter/material.dart';

class BuscarDocente extends StatelessWidget {
  const BuscarDocente({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Buscar Docente');
  }
}