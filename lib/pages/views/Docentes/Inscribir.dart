import 'package:flutter/material.dart';

class InscribirDocente extends StatelessWidget {
  const InscribirDocente({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Inscribir Docente');
  }
}