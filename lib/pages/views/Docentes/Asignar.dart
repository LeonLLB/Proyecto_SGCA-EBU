import 'package:flutter/material.dart';

class AsignarDocente extends StatelessWidget {
  const AsignarDocente({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Asignar Docente');
  }
}