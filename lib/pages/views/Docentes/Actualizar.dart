import 'package:flutter/material.dart';

class ActualizarDocente extends StatelessWidget {
  const ActualizarDocente({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Actualizar Docente');
  }
}